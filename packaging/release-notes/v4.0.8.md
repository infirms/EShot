## EShot v4.0.8

This hotfix resolves a Linux video recording issue introduced by slow GStreamer plugin discovery.

### Changes

- Fixed false `No GStreamer AAC encoder is installed` errors when an installed encoder takes longer to initialize
- Prevented timed-out GStreamer inspection processes from being left running

This issue only affected the Linux Wayland recording backend. Windows recording is unchanged.
